{{#each topics.topic_data}}
  <div class='row align-center mb-5'>
    <a href='/topic/{{this.id}}'>
      <div class='col-md-6'>
        <h2>{{this.subject}}</h2>
      </div>
      <div class='col-md-6'>
        <h3>{{this.contents}}</h3>
      </div>
    </a>
  </div>
{{/each}}